package ru.university.hits.code.module_5;

/*
#include <iostream>
#include <string>
#include <regex>

int main() {
    std::regex pattern("(([0-1][0-9])|([0-2][0-3])):([0-5][0-9])");
    std::string input;
    std::string line;
    std::getline(std::cin, line, static_cast<char>(EOF));

    std::sregex_iterator next(line.begin(), line.end(), pattern);
    std::sregex_iterator end;
    for (; next != end; next++) {
        input += next->str() + '\n';
    }

    std::cout << input;
    return 0;
}
 */

class Нахождение_подстроки_вида_чч_мм {



}


